# Dev server proxy target for /api (Vite config, not exposed to browser)
# Default: http://localhost:3000 (vercel dev). Set to https://didifkup.com to hit production when running only `npm run dev`.
# API_PROXY_TARGET=

# Client (Vite — exposed to browser)
# Set to your deployed URL for local dev if not running vercel dev (e.g. https://didifkup.vercel.app)
VITE_API_BASE_URL=
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_STRIPE_PUBLISHABLE_KEY=
VITE_STRIPE_PAYMENT_LINK_URL=
# VITE_STRIPE_PAYMENT_LINK=   # alternative name (optional)

# Server (Vercel / API — never expose to client)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_PRO_MONTHLY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_URL=
SITE_URL=
OPENAI_API_KEY=
